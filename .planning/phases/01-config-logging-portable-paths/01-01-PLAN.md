---
phase: 01-config-logging-portable-paths
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.py
  - linker_trigger.py
  - main.py
autonomous: true
requirements: [BUGFIX-03, BUGFIX-04, DX-01, DX-02]
user_setup: []

must_haves:
  truths:
    - "config.py no longer creates directories or copies .env at import time — only after explicit init() call"
    - "linker_trigger.py uses config.OBSIDIAN_LINKER_PATH instead of hardcoded absolute path"
    - "linker_trigger.py output goes to logger (filterable, timestamped) instead of raw print()"
    - "main.py calls config.init() on startup and uses logger for TTS diagnostics"
    - "OBSIDIAN_LINKER_PATH env var can override the default fallback path"
  artifacts:
    - path: "config.py"
      provides: "init() function, OBSIDIAN_LINKER_PATH constant"
      contains: "def init():"
    - path: "linker_trigger.py"
      provides: "Uses config.OBSIDIAN_LINKER_PATH, no hardcoded paths, uses logger"
      lacks: "print("
    - path: "main.py"
      provides: "calls config.init() in main(), uses logger for TTS"
      contains: "config.init("
  key_links:
    - from: "main.py"
      to: "config.init()"
      via: "config.init() call at startup"
      pattern: "config\\.init\\(\\)"
    - from: "linker_trigger.py"
      to: "config.OBSIDIAN_LINKER_PATH"
      via: "import from config"
      pattern: "OBSIDIAN_LINKER_PATH"
---

<objective>
**Config Foundation: Eliminate Import-Time Side Effects, Portable Paths, and Wire Logging in linker_trigger/main**

**Purpose:** Make `config.py` safe to import without triggering I/O (directory creation, .env copy). Add `OBSIDIAN_LINKER_PATH` env var to replace the hardcoded Windows path in `linker_trigger.py`. Wire `config.init()` into `main.py` startup. Replace `print()` with `logger` in `linker_trigger.py` and `main.py`.

**Output:** Modified `config.py` with `init()` function and `OBSIDIAN_LINKER_PATH`, modified `linker_trigger.py` using config path + logger, modified `main.py` calling `config.init()` + logging TTS diagnostics.
</objective>

<execution_context>
@C:/Users/saraf/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/saraf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/intel/CONCERNS.md
@.planning/intel/PATTERNS.md

@config.py
@linker_trigger.py
@main.py

<interfaces>
From config.py (current module-level constants, all kept at module level):
- BASE_DIR: Path = Path(__file__).resolve().parent
- ENV_PATH: Path = BASE_DIR / ".env"
- LINKER_ENV_PATH: Path = BASE_DIR.parent / "obsidian-linker" / ".env"
- INPUT_PDF_DIR: Path = BASE_DIR / "inputPDF"
- OUTPUT_DIR: Path = BASE_DIR / "output"
- OUTPUT_TEXT: Path = OUTPUT_DIR / "output.txt"
- OBSIDIAN_VAULT_DIR: Path = Path(os.getenv("OBSIDIAN_VAULT_DIR", str(BASE_DIR / "ObsidianVault")))
- OBSIDIAN_CACHE_FILE: Path = OBSIDIAN_VAULT_DIR / ".linker_cache.json"
- VECTORSTORE_DIR: Path = BASE_DIR / "faiss_index"
- FAISS_INDEX_FILE: Path = VECTORSTORE_DIR / "index.faiss"
- EMBEDDING_MODEL: str = "models/gemini-embedding-2"
- LLM_MODEL: str = os.getenv("LLM_MODEL", "llama-3.3-70b-versatile")
- RETRIEVAL_K: int = 3
- CHUNK_SIZE: int = 500
- CHUNK_OVERLAP: int = 100
- MIN_IMAGE_SIZE: int = 150

Current import-time side effects (all move into init()):
- Lines 17-23: .env copy from LINKER_ENV_PATH to ENV_PATH
- Line 26: load_dotenv(dotenv_path=ENV_PATH) — KEEP at module level
- Line 30: INPUT_PDF_DIR.mkdir(parents=True, exist_ok=True)
- Line 33: OUTPUT_DIR.mkdir(parents=True, exist_ok=True)

From linker_trigger.py:
- Hardcoded path at lines 12 and 44: `r"c:\Users\saraf\Desktop\projects\obsidian-linker\main.py"`
- Function signatures: `trigger_obsidian_linker() -> bool`, `run_obsidian_linker_sync() -> bool`
- Uses: `subprocess.Popen(args, ...)`, `subprocess.run(args, ...)`
- Import: `from config import OBSIDIAN_VAULT_DIR`
- 10 print() calls total (lines 15, 32, 35, 47, 51, 63, 66, 69, 74, 76)

From main.py:
- Function: `def main():` at line 63
- 6 print() calls (lines 371, 376, 380, 382, 384, 386) — all TTS diagnostics
- No logger defined
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create config.init() — consolidate I/O side effects</name>
  <files>config.py</files>
  <read_first>config.py</read_first>
  <action>
    Move the I/O side effects currently at module level into a new `def init() -> None:` function placed after all constant definitions but before the `if __name__ == "__main__"` block:

    1. Keep at module level (unchanged):
       - `load_dotenv(dotenv_path=ENV_PATH)` — must remain for constants that use `os.getenv`
       - All constant definitions (BASE_DIR, ENV_PATH, LINKER_ENV_PATH, INPUT_PDF_DIR, OUTPUT_DIR, etc.)
       - `logging.basicConfig(...)` and `logger = logging.getLogger(__name__)`

    2. Move into `def init():`:
       - The .env copy block (current lines 17-23):
         ```python
         if not ENV_PATH.exists() and LINKER_ENV_PATH.exists():
             try:
                 ENV_PATH.write_text(LINKER_ENV_PATH.read_text(encoding="utf-8"), encoding="utf-8")
                 logger.info("Copied .env successfully from obsidian-linker project.")
             except Exception as e:
                 logger.warning(f"Could not copy .env from obsidian-linker: {e}")
         ```
       - `INPUT_PDF_DIR.mkdir(parents=True, exist_ok=True)` (current line 30)
       - `OUTPUT_DIR.mkdir(parents=True, exist_ok=True)` (current line 33)

    3. For the logger calls inside init(), strip the `[CONFIG]` prefix — logger already provides module name in the `%(name)s` format.
  </action>
  <acceptance_criteria>
    - `config.py` has a function `def init():` that is called explicitly, not triggered by import
    - Importing `config` alone does not call `.mkdir()` or copy `.env`
    - `load_dotenv()` remains at module level, not inside init()
    - All 11 constant definitions remain at module level
    - The `init()` function body contains exactly: .env copy conditional, INPUT_PDF_DIR.mkdir(), OUTPUT_DIR.mkdir()
  </acceptance_criteria>
  <verify>
    <automated>python -c "import ast; tree = ast.parse(open('config.py').read()); funcs = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)]; assert 'init' in funcs, 'init() function not found'</automated>
  </verify>
  <done>config.py has explicit init() function; import-time side effects eliminated; all constants remain at module level</done>
</task>

<task type="auto">
  <name>Task 2: Add OBSIDIAN_LINKER_PATH to config.py + fix linker_trigger.py</name>
  <files>config.py, linker_trigger.py</files>
  <read_first>config.py, linker_trigger.py</read_first>
  <action>
    **Part A — config.py:**
    Insert after the `OBSIDIAN_CACHE_FILE` definition:
    ```python
    OBSIDIAN_LINKER_PATH: Path = Path(os.getenv("OBSIDIAN_LINKER_PATH", str(BASE_DIR.parent / "obsidian-linker" / "main.py")))
    ```
    This reads the `OBSIDIAN_LINKER_PATH` env var, defaulting to the sibling project path.

    **Part B — linker_trigger.py (path fix):**
    1. Change imports: replace `from config import OBSIDIAN_VAULT_DIR` with `from config import OBSIDIAN_VAULT_DIR, OBSIDIAN_LINKER_PATH`
    2. Replace line 12 `linker_main = r"c:\Users\saraf\Desktop\projects\obsidian-linker\main.py"` with `linker_main = str(OBSIDIAN_LINKER_PATH)`
    3. Replace line 44 (same hardcoded path) with `linker_main = str(OBSIDIAN_LINKER_PATH)`
    4. Replace `if not os.path.exists(linker_main):` (line 14 and 46) with `if not OBSIDIAN_LINKER_PATH.exists():`
    5. Keep `args = [sys.executable, linker_main, ...]` — `linker_main` variable remains for minimal diff, its value now comes from config

    **Part C — linker_trigger.py (print→logger migration):**
    1. Add `import logging` at the top
    2. Add `logger = logging.getLogger(__name__)` after imports
    3. Replace all 10 print() calls:
       - `print(f"[Linker Trigger Error] ...")` at lines 15, 35, 47, 66, 69 → `logger.error(...)`
         - Line 15: `logger.error("Obsidian Linker main.py not found at: %s", OBSIDIAN_LINKER_PATH)`
         - Line 35: `logger.error("Failed to launch background linker: %s", e)`
         - Line 47: `logger.error("Obsidian Linker main.py not found at: %s", OBSIDIAN_LINKER_PATH)`
         - Line 66: `logger.error("Linker failed with code %s: %s", result.returncode, result.stderr)`
         - Line 69: `logger.error("Failed to run linker synchronously: %s", e)`
       - `print(f"[Linker Trigger] ...")` at lines 32, 51, 63 → `logger.info(...)`
         - Line 32: `logger.info("Obsidian Linker triggered successfully in background for: %s", OBSIDIAN_VAULT_DIR)`
         - Line 51: `logger.info("Starting synchronous Obsidian Linker run for: %s...", OBSIDIAN_VAULT_DIR)`
         - Line 63: `logger.info("Obsidian Linker completed successfully.")`
       - Lines 74, 76 (inside `if __name__ == "__main__":`) → `logger.info(...)`
         - `logger.info("Testing linker trigger...")`
         - `logger.info("Trigger result: %s", success)`
    4. Strip all `[Linker Trigger]` / `[Linker Trigger Error]` prefix text from message strings — logger adds module name via `%(name)s`
  </action>
  <acceptance_criteria>
    - `config.py` contains `OBSIDIAN_LINKER_PATH` constant defined via `os.getenv("OBSIDIAN_LINKER_PATH", ...)`
    - `linker_trigger.py` imports `OBSIDIAN_LINKER_PATH` from config
    - `linker_trigger.py` contains zero hardcoded path strings matching `c:\Users\saraf\Desktop\projects\obsidian-linker\main.py`
    - `linker_trigger.py` uses `OBSIDIAN_LINKER_PATH.exists()` for path checking
    - `linker_trigger.py` has `logger = logging.getLogger(__name__)` and zero `print(` calls remaining
    - `if __name__ == "__main__":` block uses `logger.info()` not `print()`
  </acceptance_criteria>
  <verify>
    <automated>python -c "import ast; tree = ast.parse(open('config.py').read()); consts = [n.targets[0].id for n in ast.walk(tree) if isinstance(n, ast.Assign) and isinstance(n.targets[0], ast.Name)]; assert 'OBSIDIAN_LINKER_PATH' in consts"</automated>
  </verify>
  <done>OBSIDIAN_LINKER_PATH is configurable via env var; linker_trigger.py uses config path and has zero print() calls</done>
</task>

<task type="auto">
  <name>Task 3: Wire config.init() into main.py + replace print→logger</name>
  <files>main.py</files>
  <read_first>main.py</read_first>
  <action>
    **Part A — Add init() call:**
    1. After the existing imports, add `import config` as a new import line (keep existing imports, no reordering needed)
    2. Inside `def main():` at line 63, add `config.init()` as the very first statement BEFORE `st.set_page_config(...)` — this ensures directories exist before Streamlit tries to access them

    **Part B — Add logger:**
    1. Add `import logging` to the top imports
    2. Add `logger = logging.getLogger(__name__)` after imports

    **Part C — Replace print() calls (6 total):**
    - Line 371: `print(f"[TTS DEBUG] tts_read_aloud active: ...")` → `logger.debug("tts_read_aloud active: %s", st.session_state.get('tts_read_aloud', False))`
    - Line 376: `print(f"[TTS DEBUG] Cleaned text for TTS: '{cleaned}'")` → `logger.debug("Cleaned text for TTS: '%s'", cleaned)`
    - Line 380: `print(f"[TTS DEBUG] Calling text_to_speech with rate: '{rate_str}'")` → `logger.debug("Calling text_to_speech with rate: '%s'", rate_str)`
    - Line 382: `print(f"[TTS DEBUG] Successfully generated ... bytes of audio")` → `logger.debug("Successfully generated %s bytes of audio", len(audio_bytes) if audio_bytes else 0)`
    - Line 384: `print("[TTS DEBUG] Cleaned text is empty. Skipping TTS.")` → `logger.debug("Cleaned text is empty. Skipping TTS.")`
    - Line 386: `print(f"[TTS Error] {e}")` → `logger.error("TTS failed: %s", e)`
    
    Strip all `[TTS DEBUG]` / `[TTS Error]` prefixes — logger provides module name.
  </action>
  <acceptance_criteria>
    - `main.py` imports `config` and has `import logging` + `logger = logging.getLogger(__name__)`
    - `main()` calls `config.init()` before `st.set_page_config()`
    - `main.py` contains zero `print(` calls after the changes (verify with grep)
    - The 6 TTS print() calls are replaced with `logger.debug()` (5) and `logger.error()` (1) — no info() for debug-level TTS diagnostics
  </acceptance_criteria>
  <verify>
    <automated>python -c "import ast; tree = ast.parse(open('main.py').read()); prints = [n for n in ast.walk(tree) if isinstance(n, ast.Call) and isinstance(n.func, ast.Name) and n.func.id == 'print']; assert len(prints) == 0, f'Found {len(prints)} print() calls remaining'"</automated>
  </verify>
  <done>config.init() called from main.py startup; all TTS diagnostic prints replaced with logger.debug/error</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.py init() | Module-level I/O (mkdir, file copy) moves from import-time to explicit call — no new trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | linker_trigger.py subprocess call | accept | `OBSIDIAN_LINKER_PATH` is user-configured; subprocess runs a known script; low-value target |
| T-01-SC | Tampering | pip installs | mitigate | No new packages introduced; existing packages unchanged |
</threat_model>

<verification>
1. `python -c "import config"` completes without creating directories or printing to stdout
2. `python -c "from config import OBSIDIAN_LINKER_PATH; print(OBSIDIAN_LINKER_PATH)"` shows expected default path
3. `python -c "from linker_trigger import trigger_obsidian_linker; print('OK')"` imports without error
4. All grep checks for `print(` in linker_trigger.py and main.py return zero matches
</verification>

<success_criteria>
1. config.py has explicit `init()` function — import-time side effects removed ✓
2. OBSIDIAN_LINKER_PATH constant exists in config.py ✓
3. linker_trigger.py uses config path (no hardcoded `c:\Users\saraf\...`) ✓
4. linker_trigger.py uses logger (zero print() calls) ✓
5. main.py calls config.init() on startup ✓
6. main.py uses logger for TTS diagnostics (zero print() calls) ✓
7. Application can start normally with these changes
</success_criteria>
