---
phase: 01-config-logging-portable-paths
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - text_chunker.py
  - graph_rag.py
autonomous: true
requirements: [DX-01]
user_setup: []

must_haves:
  truths:
    - "text_chunker.py output goes through logger (filterable, timestamped) instead of print()"
    - "graph_rag.py output goes through logger instead of print()"
  artifacts:
    - path: "text_chunker.py"
      provides: "Has logger definition, all output via logger (no print() calls)"
      contains: "logger = logging.getLogger(__name__)"
    - path: "graph_rag.py"
      provides: "Has logger definition, all output via logger (no print() calls)"
      contains: "logger = logging.getLogger(__name__)"
  key_links:
    - from: "text_chunker.py"
      to: "logger"
      via: "new logger = logging.getLogger(__name__), print→logger replacement"
    - from: "graph_rag.py"
      to: "logger"
      via: "new logger = logging.getLogger(__name__), print→logger replacement"
---

<objective>
**Print→Logger Migration — Remaining Modules (text_chunker + graph_rag)**

**Purpose:** Replace all unstructured `print()` calls in `text_chunker.py` (~33 calls) and `graph_rag.py` (~14 calls) with structured `logger` calls at appropriate severity levels. Both modules currently have zero logging — add `logger` definition and migrate every `print()`.

**Output:** Modified `text_chunker.py` and `graph_rag.py` with zero `print()` calls, all diagnostic output routed through Python's logging framework.
</objective>

<execution_context>
@C:/Users/saraf/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/saraf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@config.py
@text_chunker.py
@graph_rag.py

<interfaces>
From config.py:
- `logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(name)s - %(levelname)s - %(message)s')`
  This single call in config.py configures all loggers project-wide. Each module just adds `logger = logging.getLogger(__name__)`.

text_chunker.py current state:
- No logger defined
- ~33 print() calls — heaviest print user in the codebase
- Already imports from config: `OUTPUT_DIR, CHUNK_SIZE, CHUNK_OVERLAP, EMBEDDING_MODEL, VECTORSTORE_DIR, INPUT_PDF_DIR`
- Has `import os` at line 1 — add `import logging` next to it
- Message formats: `[ERROR]`, `[WARNING]`, `[INFO]`, `[SUCCESS]`
- Has `load_dotenv()` call at line 16 — redundant since config.py already calls it, but leave it for now (out of scope)

graph_rag.py current state:
- No logger defined
- ~14 print() calls
- Already has `import os`, `from pathlib import Path` — add `import logging`
- Message formats: `[Warning]`, `[GraphRAG]`, `[Error]`, `[GraphRAG Warning]`, `[GraphRAG Error]`, `[GraphRAG Fallback]`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add logger + replace print() in text_chunker.py</name>
  <files>text_chunker.py</files>
  <read_first>text_chunker.py</read_first>
  <action>
    **Part A — Add logger:**
    1. Add `import logging` to the imports (line 1 area with `import os`)
    2. Add `logger = logging.getLogger(__name__)` right after the import block (before the `load_dotenv()` call)

    **Part B — Replace print() calls (~33 occurrences):**

    Replace each print() with logger using `%s` formatting. Strip all `[ERROR]`, `[WARNING]`, `[INFO]`, `[SUCCESS]` prefix text. Strip leading `\n` prefixes.

    | Line(s) | Current print() | Replacement |
    |---------|----------------|-------------|
    | 32 | `print(f"[ERROR] PDF directory not found: {input_pdf_dir.absolute()}")` | `logger.error("PDF directory not found: %s", input_pdf_dir.absolute())` |
    | 36 | `print(f"[ERROR] Cache directory not found: {cache_dir.absolute()}")` | `logger.error("Cache directory not found: %s", cache_dir.absolute())` |
    | 45 | `print("[WARNING] No PDF files found to index.")` | `logger.warning("No PDF files found to index.")` |
    | 67 | `print(f"[WARNING] No cached text found for: {pdf_path.name}")` | `logger.warning("No cached text found for: %s", pdf_path.name)` |
    | 70 | `print(f"[INFO] Parsing cache file: {cache_file.name}")` | `logger.info("Parsing cache file: %s", cache_file.name)` |
    | 96 | `print(f"[ERROR] Failed to read or parse cache for {pdf_path.name}: {e}")` | `logger.error("Failed to read or parse cache for %s: %s", pdf_path.name, e)` |
    | 99 | `print("[WARNING] No text segments found to chunk.")` | `logger.warning("No text segments found to chunk.")` |
    | 102 | `print(f"[INFO] Initializing token-aware text splitter (Size: {chunk_size}, Overlap: {chunk_overlap})...")` | `logger.info("Initializing token-aware text splitter (Size: %s, Overlap: %s)...", chunk_size, chunk_overlap)` |
    | 111 | `print("[INFO] Splitting text into metadata-aware documents...")` | `logger.info("Splitting text into metadata-aware documents...")` |
    | 125 | `print(f"[INFO] Initializing Google Embeddings Model ({EMBEDDING_MODEL})...")` | `logger.info("Initializing Google Embeddings Model (%s)...", EMBEDDING_MODEL)` |
    | 133 | `print(f"[ERROR] Failed to initialize embeddings model. Make sure GOOGLE_API_KEY is set. Details: {e}")` | `logger.error("Failed to initialize embeddings model. Make sure GOOGLE_API_KEY is set. Details: %s", e)` |
    | 141 | `print(f"[INFO] Generating embeddings for {len(chunks)} chunks concurrently with safe throttling...")` | `logger.info("Generating embeddings for %s chunks concurrently with safe throttling...", len(chunks))` |
    | 156 | `print(f"\n[WARNING] Rate limit hit on chunk {idx+1} (Attempt {attempt+1}/5). Backing off for {delay}s...")` | `logger.warning("Rate limit hit on chunk %s (Attempt %s/5). Backing off for %ss...", idx + 1, attempt + 1, delay)` |
    | 161 | `print(f"\n[ERROR] Failed to embed chunk {idx+1} after 5 attempts: {last_error}")` | `logger.error("Failed to embed chunk %s after 5 attempts: %s", idx + 1, last_error)` |
    | 171 | `print("[INFO] All embeddings generated successfully.")` | `logger.info("All embeddings generated successfully.")` |
    | 183 | `print(f"\n[ERROR] FAISS creation failed: {e}")` | `logger.error("FAISS creation failed: %s", e)` |
    | 196 | `print("[SUCCESS] All PDFs removed. Cleared FAISS index directory.")` | `logger.info("All PDFs removed. Cleared FAISS index directory.")` |
    | 198 | `print("[INFO] No PDFs found and no index to clear.")` | `logger.info("No PDFs found and no index to clear.")` |
    | 207 | `print("[INFO] Existing FAISS index detected. Checking currently indexed PDFs...")` | `logger.info("Existing FAISS index detected. Checking currently indexed PDFs...")` |
    | 224 | `print(f"[INFO] Loaded existing FAISS index. Indexed PDFs: {existing_sources}")` | `logger.info("Loaded existing FAISS index. Indexed PDFs: %s", existing_sources)` |
    | 226 | `print(f"[WARNING] Failed to load existing FAISS index: {e}. Will rebuild index from scratch.")` | `logger.warning("Failed to load existing FAISS index: %s. Will rebuild index from scratch.", e)` |
    | 243-247 | `print(f"[INFO] Sync Plan:")` + 4 lines of `print(f"       - ...")` | Replace the block (lines 243-247, the 5 print calls) with: `logger.info("Sync Plan:\n       - Active PDFs: %s\n       - Already Indexed: %s\n       - To Delete/Re-index: %s\n       - To Generate/Add: %s", active_pdfs, existing_sources, sources_to_delete, sources_to_index)` |
    | 251 | `print("[INFO] FAISS vector store is already perfectly up to date. Skipping re-indexing.")` | `logger.info("FAISS vector store is already perfectly up to date. Skipping re-indexing.")` |
    | 263 | `print(f"[SUCCESS] Deleted {len(ids_to_delete)} old chunks from the index.")` | `logger.info("Deleted %s old chunks from the index.", len(ids_to_delete))` |
    | 266 | `print("[INFO] Index is now empty after deletions.")` | `logger.info("Index is now empty after deletions.")` |
    | 269 | `print(f"[WARNING] Failed to delete chunks from existing index: {e}. Will rebuild index from scratch.")` | `logger.warning("Failed to delete chunks from existing index: %s. Will rebuild index from scratch.", e)` |
    | 274 | `print(f"[INFO] Chunking new/modified documents: {sources_to_index}...")` | `logger.info("Chunking new/modified documents: %s...", sources_to_index)` |
    | 278 | `print("[WARNING] No text chunks generated for the new/modified documents.")` | `logger.warning("No text chunks generated for the new/modified documents.")` |
    | 280 | `print(f"[SUCCESS] Generated {len(chunks)} chunks to index.")` | `logger.info("Generated %s chunks to index.", len(chunks))` |
    | 281 | `print("[INFO] Building embeddings for new chunks...")` | `logger.info("Building embeddings for new chunks...")` |
    | 285 | `print("[ERROR] Failed to generate embeddings for new chunks.")` | `logger.error("Failed to generate embeddings for new chunks.")` |
    | 293 | `print("[INFO] Merging new chunks into the existing FAISS index...")` | `logger.info("Merging new chunks into the existing FAISS index...")` |
    | 295 | `print("[SUCCESS] Merged new chunks successfully.")` | `logger.info("Merged new chunks successfully.")` |
    | 301 | `print(f"[SUCCESS] FAISS vector store successfully saved to '{VECTORSTORE_DIR}'!")` | `logger.info("FAISS vector store successfully saved to '%s'!", VECTORSTORE_DIR)` |
    | 304 | `print(f"[ERROR] Failed to save FAISS index: {e}")` | `logger.error("Failed to save FAISS index: %s", e)` |
    | 311 | `print("[SUCCESS] Vector store is now completely empty.")` | `logger.info("Vector store is now completely empty.")` |

    Message level conventions:
    - flow/progress/status → `logger.info()`
    - warnings/rate-limits/expected edge cases → `logger.warning()`
    - failures/exceptions → `logger.error()`
    - `[SUCCESS]` messages are `info()` level (not debug — still useful in normal operation)
  </action>
  <acceptance_criteria>
    - `text_chunker.py` has `import logging` and `logger = logging.getLogger(__name__)`
    - `text_chunker.py` has zero `print(` calls remaining
    - All messages use `logger.%s` formatting, not f-string concatenation
    - No `[ERROR]`, `[WARNING]`, `[INFO]`, `[SUCCESS]` prefix text remains in any message
    - The 5-line Sync Plan block is consolidated into a single `logger.info()` call
  </acceptance_criteria>
  <verify>
    <automated>python -c "import ast; t=ast.parse(open('text_chunker.py').read()); p=[n for n in ast.walk(t) if isinstance(n,ast.Call) and isinstance(n.func,ast.Name) and n.func.id=='print']; assert len(p)==0, f'{len(p)} print() calls remaining'"</automated>
  </verify>
  <done>text_chunker.py has logger defined and zero print() calls</done>
</task>

<task type="auto">
  <name>Task 2: Add logger + replace print() in graph_rag.py</name>
  <files>graph_rag.py</files>
  <read_first>graph_rag.py</read_first>
  <action>
    **Part A — Add logger:**
    1. Add `import logging` to the existing import block (after `import json` or with other standard library imports)
    2. Add `logger = logging.getLogger(__name__)` after the import block (before the `MatchedNotes` class definition)

    **Part B — Replace print() calls (~14 occurrences):**

    | Line(s) | Current print() | Replacement |
    |---------|----------------|-------------|
    | 28 | `print(f"[Warning] Obsidian linker cache file not found at: {self.cache_file}")` | `logger.warning("Obsidian linker cache file not found at: %s", self.cache_file)` |
    | 46 | `print(f"[GraphRAG] Loaded cache successfully. Found {len(self.available_notes)} notes in index (mtime: {mtime}).")` | `logger.info("Loaded cache successfully. Found %s notes in index (mtime: %s).", len(self.available_notes), mtime)` |
    | 48 | `print(f"[Error] Failed to load Obsidian linker cache: {e}")` | `logger.error("Failed to load Obsidian linker cache: %s", e)` |
    | 54 | `print(f"[GraphRAG Warning] Obsidian concept FAISS index not found at: {faiss_path}")` | `logger.warning("Obsidian concept FAISS index not found at: %s", faiss_path)` |
    | 65 | `print("[GraphRAG Error] GOOGLE_API_KEY not found in environment.")` | `logger.error("GOOGLE_API_KEY not found in environment.")` |
    | 76 | `print(f"[GraphRAG Error] Failed to load Obsidian concept FAISS index: {e}")` | `logger.error("Failed to load Obsidian concept FAISS index: %s", e)` |
    | 91 | `print(f"[GraphRAG] Running similarity search on Obsidian FAISS index for: '{question}'")` | `logger.info("Running similarity search on Obsidian FAISS index for: '%s'", question)` |
    | 96 | `print(f"[GraphRAG FAISS Search] Note concept: {doc.metadata.get('note')}, Score (L2 Distance): {score:.4f}")` | `logger.info("Note concept: %s, Score (L2 Distance): %.4f", doc.metadata.get('note'), score)` |
    | 102 | `print(f"[GraphRAG] Vector pre-filtering found {len(candidate_notes)} candidate notes: {candidate_notes}")` | `logger.info("Vector pre-filtering found %s candidate notes: %s", len(candidate_notes), candidate_notes)` |
    | 117 | `print(f"[GraphRAG Fallback] Selected {len(candidate_notes)} fallback candidates: {candidate_notes}")` | `logger.info("Selected %s fallback candidates: %s", len(candidate_notes), candidate_notes)` |
    | 128 | `print("[GraphRAG Error] GROQ_API_KEY not found in environment.")` | `logger.error("GROQ_API_KEY not found in environment.")` |
    | 163 | `print(f"[GraphRAG] Selected relevant notes: {matched}")` | `logger.info("Selected relevant notes: %s", matched)` |
    | 166 | `print(f"[GraphRAG Error] Failed to select relevant notes via LLM: {e}")` | `logger.error("Failed to select relevant notes via LLM: %s", e)` |
    | 232 | `print(f"[GraphRAG Error] Failed to read note {note_name}: {e}")` | `logger.error("Failed to read note %s: %s", note_name, e)` |

    Strip all `[Warning]`, `[GraphRAG]`, `[Error]`, `[GraphRAG Warning]`, `[GraphRAG Error]`, `[GraphRAG Fallback]`, `[GraphRAG FAISS Search]` prefix text. Logger already provides module name.
  </action>
  <acceptance_criteria>
    - `graph_rag.py` has `import logging` and `logger = logging.getLogger(__name__)`
    - `graph_rag.py` has zero `print(` calls remaining
    - All messages use `logger.%s` formatting, not f-string concatenation
    - No `[Warning]`, `[GraphRAG]`, `[Error]`, `[GraphRAG Warning]`, `[GraphRAG Error]`, `[GraphRAG Fallback]` prefix text remains
  </acceptance_criteria>
  <verify>
    <automated>python -c "import ast; t=ast.parse(open('graph_rag.py').read()); p=[n for n in ast.walk(t) if isinstance(n,ast.Call) and isinstance(n.func,ast.Name) and n.func.id=='print']; assert len(p)==0, f'{len(p)} print() calls remaining'"</automated>
  </verify>
  <done>graph_rag.py has logger defined and zero print() calls</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| text_chunker.py → logger | All diagnostic output routed through logging |
| graph_rag.py → logger | All diagnostic output routed through logging |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Information Disclosure | logger output | accept | No change in what is logged — same information, structured format. No new packages. |
</threat_model>

<verification>
1. `grep -n "print(" text_chunker.py` returns zero matches
2. `grep -n "print(" graph_rag.py` returns zero matches
3. `python -c "import ast; ast.parse(open('text_chunker.py').read())"` — syntax valid
4. `python -c "import ast; ast.parse(open('graph_rag.py').read())"` — syntax valid
5. Both files have `logger = logging.getLogger(__name__)`
</verification>

<success_criteria>
1. text_chunker.py: logger defined, ~33 print() calls → logger calls ✓
2. graph_rag.py: logger defined, ~14 print() calls → logger calls ✓
3. All messages use structured logger.%s formatting ✓
4. All prefix tags stripped from message strings ✓
5. Both files syntax-valid ✓
</success_criteria>
