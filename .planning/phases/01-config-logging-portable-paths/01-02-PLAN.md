---
phase: 01-config-logging-portable-paths
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - rag_agent.py
  - extract_text.py
autonomous: true
requirements: [DX-01]
user_setup: []

must_haves:
  truths:
    - "rag_agent.py output no longer appears as unstructured print() — all diagnostic output goes through logger"
    - "extract_text.py output goes through logger (filterable, timestamped) instead of print()"
  artifacts:
    - path: "rag_agent.py"
      provides: "All diagnostic output via logger (no print() calls)"
      lacks: "print("
    - path: "extract_text.py"
      provides: "Has logger definition, all output via logger (no print() calls)"
      contains: "logger = logging.getLogger(__name__)"
  key_links:
    - from: "rag_agent.py"
      to: "logger"
      via: "existing logger = logging.getLogger(__name__) stays, print→logger replacement"
    - from: "extract_text.py"
      to: "logger"
      via: "new logger = logging.getLogger(__name__) added, print→logger replacement"
---

<objective>
**Print→Logger Migration — Core Modules (rag_agent + extract_text)**

**Purpose:** Replace all unstructured `print()` calls in `rag_agent.py` and `extract_text.py` with structured `logger` calls at appropriate severity levels (info, warning, error). Both modules already have some logger usage — standardize to logger-only.

**Output:** Modified `rag_agent.py` and `extract_text.py` with zero `print()` calls, all diagnostic output routed through Python's logging framework.
</objective>

<execution_context>
@C:/Users/saraf/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/saraf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@config.py
@rag_agent.py
@extract_text.py

<interfaces>
From config.py:
- logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(name)s - %(levelname)s - %(message)s')
  Already configures all loggers in the project. Every module just needs `logger = logging.getLogger(__name__)`.

rag_agent.py current state:
- Already has `logger = logging.getLogger(__name__)` at line 8
- Already uses `logger.info()` in some places (line 109: `rotate_groq_key`)
- 12 print() calls remaining (lines 202, 207, 210, 286, 330, 369, 406, 453, 508, 568, 596, 655)
- Message format: `[RAGAgent]`, `[RAGAgent Error]`, `[RAGAgent Rate Limit]`, `[Error reading note ...]`, `[RAGAgent PDF Search]`, etc.

extract_text.py current state:
- No logger defined
- 12 print() calls (lines 44, 69-72, 77, 97, 109, 121, 138, 143, 163, 166, 186, 189)
- Already imports `Path` and `tempfile` — just needs `import logging`
- Message format: `[WARNING]`, `[INFO]`, `[ERROR]`, `[SUCCESS]`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace print() with logger in rag_agent.py</name>
  <files>rag_agent.py</files>
  <read_first>rag_agent.py</read_first>
  <action>
    Keep existing `logger = logging.getLogger(__name__)` at line 8 and all existing `logger.info()` calls (e.g., line 109 key rotation log).

    Replace each print() with the appropriate logger level. Use logger's `%s` formatting — NOT f-string concatenation — to keep with lazy-evaluation best practice.

    Replacement table (12 occurrences):

    | Line | Current print() | Replacement |
    |------|----------------|-------------|
    | 202 | `print(f"[RAGAgent] Reformulated conversational query: '{question}' -> '{standalone.strip()}'")` | `logger.info("Reformulated conversational query: '%s' -> '%s'", question, standalone.strip())` |
    | 207 | `print(f"[RAGAgent Rate Limit] Standalone query hit rate limit on attempt {attempt + 1}. Rotating key and retrying...")` | `logger.warning("Standalone query hit rate limit on attempt %s. Rotating key and retrying...", attempt + 1)` |
    | 210 | `print(f"[RAGAgent Error] Failed to reformulate conversational query: {e}")` | `logger.error("Failed to reformulate conversational query: %s", e)` |
    | 286 | `print(f"[Error reading note {note_file.name} for checkboxes]: {e}")` | `logger.error("Error reading note '%s' for checkboxes: %s", note_file.name, e)` |
    | 330 | `print(f"[RAGAgent PDF Search] Chunk source: {doc.metadata.get('source')} pg {doc.metadata.get('page')}, Score (L2 Distance): {score:.4f}")` | `logger.info("Chunk source: %s pg %s, Score (L2 Distance): %.4f", doc.metadata.get('source'), doc.metadata.get('page'), score)` |
    | 369 | `print(f"[RAGAgent Rate Limit] Hit rate limit on attempt {attempt + 1}. Rotating key and retrying...")` | `logger.warning("Hit rate limit on attempt %s. Rotating key and retrying...", attempt + 1)` |
    | 406 | same format as 286 | `logger.error("Error reading note '%s' for checkboxes: %s", note_file.name, e)` |
    | 453 | `print(f"[RAGAgent PDF Search Stream] Chunk source: {doc.metadata.get('source')} pg {doc.metadata.get('page')}, Score (L2 Distance): {score:.4f}")` | `logger.info("Chunk source: %s pg %s, Score (L2 Distance): %.4f", doc.metadata.get('source'), doc.metadata.get('page'), score)` |
    | 508 | `print(f"[RAGAgent Rate Limit] Stream hit rate limit on attempt {attempt + 1}. Rotating key and retrying...")` | `logger.warning("Stream hit rate limit on attempt %s. Rotating key and retrying...", attempt + 1)` |
    | 568 | `print(f"[RAGAgent Rate Limit] Note compilation hit rate limit. Rotating key and retrying...")` | `logger.warning("Note compilation hit rate limit. Rotating key and retrying...")` |
    | 596 | `print(f"[RAGAgent] Successfully wrote new note: {file_path}")` | `logger.info("Successfully wrote new note: %s", file_path)` |
    | 655 | `print(f"[RAGAgent status error] {e}")` | `logger.error("Status check error: %s", e)` |

    Strip all `[RAGAgent]`, `[RAGAgent Error]`, `[RAGAgent Rate Limit]`, `[Error reading note...]` type prefixes from message strings. Logger already provides module name (`rag_agent`) in `%(name)s`.

    Message format conventions:
    - status/flow info → `logger.info()`
    - recoverable warnings (rate limits, expected edge cases) → `logger.warning()`
    - failures (exceptions, errors) → `logger.error()` — never `print(f"[ERROR] ...")`
  </action>
  <acceptance_criteria>
    - `rag_agent.py` has zero `print(` calls remaining
    - All existing logger calls (e.g., line 109) are preserved unchanged
    - All messages use `logger.%s` formatting, not f-string concatenation
    - No `[RAGAgent]`, `[RAGAgent Error]`, `[RAGAgent Rate Limit]` prefix text remains in messages
  </acceptance_criteria>
  <verify>
    <automated>python -c "import ast; t=ast.parse(open('rag_agent.py').read()); p=[n for n in ast.walk(t) if isinstance(n,ast.Call) and isinstance(n.func,ast.Name) and n.func.id=='print']; assert len(p)==0, f'{len(p)} print() calls remaining'"</automated>
  </verify>
  <done>rag_agent.py has zero print() calls; all diagnostic output uses logger</done>
</task>

<task type="auto">
  <name>Task 2: Add logger + replace print() in extract_text.py</name>
  <files>extract_text.py</files>
  <read_first>extract_text.py</read_first>
  <action>
    **Part A — Add logger:**
    1. Add `import logging` to the existing import block at the top
    2. Add `logger = logging.getLogger(__name__)` after the import block (before or after the first constant-like section — place it after `from config import ...` line)

    **Part B — Replace print() calls (12 occurrences):**

    | Line | Current print() | Replacement |
    |------|----------------|-------------|
    | 44 | `print(f"[WARNING] Table extraction failed (page {page.number + 1}): {e}")` | `logger.warning("Table extraction failed (page %s): %s", page.number + 1, e)` |
    | 69-72 | `print(f"[INFO] Page {page.number + 1}: running image OCR ({img_index + 1}/{len(images)})...")` | `logger.info("Page %s: running image OCR (%s/%s)...", page.number + 1, img_index + 1, len(images))` |
    | 77 | `print(f"[WARNING] Image OCR skipped (page {page.number + 1}, image {img_index + 1}): {e}")` | `logger.warning("Image OCR skipped (page %s, image %s): %s", page.number + 1, img_index + 1, e)` |
    | 97 | `print(f"[ERROR] Failed to extract page {page_num} for {pdf_path.name}: {e}")` | `logger.error("Failed to extract page %s for %s: %s", page_num, pdf_path.name, e)` |
    | 109 | `print(f"[INFO] Extracting {num_pages} pages from {pdf_path.name} concurrently...")` | `logger.info("Extracting %s pages from %s concurrently...", num_pages, pdf_path.name)` |
    | 121 | `print(f"[ERROR] Page worker thread exception on page {p_num}: {e}")` | `logger.error("Page worker thread exception on page %s: %s", p_num, e)` |
    | 138 | `print(f"[ERROR] Directory not found: {input_dir}")` | `logger.error("Directory not found: %s", input_dir)` |
    | 143 | `print(f"[WARNING] No PDF files found in {input_dir}")` | `logger.warning("No PDF files found in %s", input_dir)` |
    | 163 | `print(f"[INFO] Using cached text for: {pdf_path.name}")` | `logger.info("Using cached text for: %s", pdf_path.name)` |
    | 166 | `print(f"\n[INFO] Extracting text (OCR={not skip_ocr}): {pdf_path.name}")` | `logger.info("Extracting text (OCR=%s): %s", not skip_ocr, pdf_path.name)` |
    | 186 | `print(f"\n[SUCCESS] Combined text saved to {output_file}")` | `logger.info("Combined text saved to %s", output_file)` |
    | 189 | `print(f"[INFO] {OUTPUT_FILE} and all caches are up to date. Skipping extraction.")` | `logger.info("%s and all caches are up to date. Skipping extraction.", OUTPUT_FILE)` |

    Strip all `[WARNING]`, `[INFO]`, `[ERROR]`, `[SUCCESS]` prefix text from messages. Strip leading `\n` prefix characters (lines 166, 186). Logger format already includes timestamp and module name.

    Message level conventions (same pattern):
    - flow/cache/extraction progress → `logger.info()`
    - expected anomalies (skips, no files) → `logger.warning()`
    - failures (exceptions) → `logger.error()`
  </action>
  <acceptance_criteria>
    - `extract_text.py` has `import logging` and `logger = logging.getLogger(__name__)`
    - `extract_text.py` has zero `print(` calls remaining
    - All messages use `logger.%s` formatting, not f-string concatenation
    - No `[INFO]`, `[WARNING]`, `[ERROR]`, `[SUCCESS]` prefix text remains in messages
    - No leading `\n` characters in log messages
  </acceptance_criteria>
  <verify>
    <automated>python -c "import ast; t=ast.parse(open('extract_text.py').read()); p=[n for n in ast.walk(t) if isinstance(n,ast.Call) and isinstance(n.func,ast.Name) and n.func.id=='print']; assert len(p)==0, f'{len(p)} print() calls remaining'"</automated>
  </verify>
  <done>extract_text.py has logger defined and zero print() calls</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rag_agent.py → logger | All diagnostic output routed through logging — same info, structured format |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Information Disclosure | logger output | accept | No change in what is logged — same information, structured format. No new packages. |
</threat_model>

<verification>
1. `grep -n "print(" rag_agent.py` returns zero matches
2. `grep -n "print(" extract_text.py` returns zero matches
3. `python -c "import ast; ast.parse(open('rag_agent.py').read())"` — syntax valid
4. `python -c "import ast; ast.parse(open('extract_text.py').read())"` — syntax valid
5. No `[RAGAgent]`, `[INFO]`, `[WARNING]`, `[ERROR]` prefix tags remain in any message string in either file
</verification>

<success_criteria>
1. rag_agent.py: 12 print() calls → 12 logger calls, existing logger calls preserved ✓
2. extract_text.py: logger defined, 12 print() calls → 12 logger calls ✓
3. All messages use structured logger.%s formatting ✓
4. All prefix tags stripped from message strings ✓
5. Both files syntax-valid ✓
</success_criteria>
